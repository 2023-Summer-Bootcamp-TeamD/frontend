export type GameInfoProps = {
  nickname: string;
  time: number;
  player_num: number;
  entry_code: string;
};

export type MakeRoomType = {
  nickname: string;
  category_id: number;
  seconds: number;
  personnel: number;
};
