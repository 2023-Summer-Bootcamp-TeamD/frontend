export type GameInfoProps = {
  nickname: string;
  time: number;
  player_num: number;
  entry_code: string;
};

export type MakeRoomType = {
  nickname: string;
  selectedCategory: number;
  seconds: number;
  personnel: number;
};
