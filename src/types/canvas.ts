export type DrawData = {
  lastX: number;
  lastY: number;
  offsetX: number;
  offsetY: number;
  isDrawing: boolean;
};

export type SelectedColorType = {
  selectedColor: string;
};

export type HandType = {
  eraseData: boolean;
};
